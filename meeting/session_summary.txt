2026-04-20 session summary

- Goal shifted to professor alignment:
  - remove Gemini's `prove(...) => proved` layer
  - keep real transitions as `rl/crl`
  - avoid `=>` inside `ceq`

- Stable state reached today:
  - `prove`, `proved`, `ProofState`, `Proved` counts in `output.maude`: all 0
  - `*-ok`, `*-sub`, `Expand`, `Expand-use` are `mb/cmb ... : ValidJudgement`
  - `Steps` is `crl Steps(...) => valid`
  - `Eval-expr` is `crl Eval-expr(...) => valid`

- Main blocker found and fixed:
  - problem:
    - `Eval-expr(...) => valid` had been emitted inside `ceq ... if ...`
    - this caused `didn't expect token =>` / `no parse for statement`
  - fix:
    - `translator.ml` `translate_decd` now has rewrite-aware lowering
    - clauses depending on rewrite judgements or rewrite-dependent defs are emitted as `rl/crl`
    - equality-style conditions on rewrite-dependent defs can be turned into direct rewrite conditions

- Current generated examples:
  - `crl [eval-expr-r0] : Eval-expr(...) => valid`
  - `crl [evalexprs-r1] : $evalexprs(...) => ...`
  - `crl [evalglobals-r1] : $evalglobals(...) => ...`

- Verification after regeneration:
  - `rewrite [10000] in WASM-FIB : steps(fib-config(i32v(5))) .`
    - result ends in `i32 5`
  - `modelCheck(fib-config(i32v(5)), <> result-is(5))`
    - `true`
  - `modelCheck(fib-config(i32v(5)), [] ~ trap-seen)`
    - `true`

- Additional cleanup completed:
  - `$evalexprss` is now `crl [evalexprss-r1]`
  - its self-recursive call is also lowered to a rewrite condition
  - `$instantiate` now uses rewrite conditions for `$evalglobals`, `$evalexprs`, `$evalexprss`, and `$allocmodule`

- Remaining work:
  1. finish auto-translation of the remaining 22 execution rules
  2. reduce warnings:
     - `multiple distinct parses`
     - `assignment condition already bound`
     - `wasm-exec.maude` `MC-I` bind-before-use
3. run non-fib regression examples

- Documentation refreshed to match the current generated state:
  - `docs/translate_example.spectec`
  - `rule_0420.md`
  - `rules_0420.md`
  - `evaluation_context.md`
- Current documentation baseline:
  - no `prove/proved/ProofState/Proved`
  - `*-ok`, `*-sub`, `Expand`, `Expand-use` use `mb/cmb`
  - `Steps`, `Eval-expr`, and rewrite-aware defs use `rl/crl`
  - evaluation contexts:
    - auto-generated: `label`, `handler`, `frame`
    - not yet auto-generated: `instrs`
    - current manual support remains in `wasm-exec.maude`

2026-04-21 baseline translator session

- New direction from 0421 meeting:
  - keep `translator.ml` as the heating/cooling / optimized path
  - create a separate baseline translator `translator_bs.ml`
  - baseline target is rules with `-- Step:` premises
  - translate them as direct conditional `crl` rules without heating/cooling
  - static judgements referenced by dynamic rules should use `eq/ceq ... = valid`

- New files / split:
  - `translator_bs.ml`
  - `main_bs.ml`
  - `dune` updated so `main_bs` builds with `translator_bs`

- Baseline translator changes:
  - disabled heat/cool special handling in `translate_step_reld`
  - non-rewrite `RelD` now emits `eq/ceq ... = valid`
  - non-rewrite relation premises now emit `... == valid`

- Important baseline milestone:
  - baseline output now generates direct context rules:
    - `step-ctxt-instrs`
    - `step-ctxt-label`
    - `step-ctxt-handler`
    - `step-ctxt-frame`
  - this means the baseline translator is no longer relying on generated heat/cool rules

- Parse blocker fixed:
  - baseline `Step/pure` schema rule had used a fake placeholder state `PREM-Z`
  - this produced:
    - `bad token PREM-Z`
    - `no parse for statement`
  - fixed by replacing the placeholder with the enclosing rule state variable
  - current generated form is:
    - `step(< Z | INSTR >) => < Z | INSTRQ >`
    - with the same `Z` used in the premise rewrite condition

- Current baseline status:
  - `/tmp/output_bs.maude` loads in Maude
  - the old `PREM-Z` parse blocker is gone
  - many warnings remain
  - the remaining bind-before-use warnings line up with the previously known hard 22 execution-rule family:
    - cast/ref success rules
    - call-ref rules
    - throw / struct / array rules

- What is not done yet:
  1. baseline fib harness verification
  2. source-based catalog of all `-- Step:` rules
  3. pattern grouping of the remaining baseline warning-heavy rules

- Additional baseline harness work completed:
  - created `wasm-exec-bs.maude`
  - this is a baseline-only harness:
    - loads `output_bs.maude`
    - keeps only execution harness + fib/property modules
    - removes manual `step-*` overrides from `wasm-exec.maude`
    - removes restore/heat-cool dependence

- Source `-- Step:` catalog verified:
  - there are 5 such rules in `4.3-execution.instructions.spectec`
    - `Steps/trans`
    - `Step/ctxt-instrs`
    - `Step/ctxt-label`
    - `Step/ctxt-handler`
    - `Step/ctxt-frame`
  - baseline output covers all 5:
    - `steps-trans`
    - `step-ctxt-instrs`
    - `step-ctxt-label`
    - `step-ctxt-handler`
    - `step-ctxt-frame`

- Baseline fib verification result:
  - `rewrite [10000] in WASM-FIB-BS : steps(fib-config(i32v(5))) .`
    - `Fatal error: stack overflow`
  - `modelCheck(fib-config(i32v(5)), <> result-is(5))`
    - `Fatal error: stack overflow`
  - `modelCheck(fib-config(i32v(5)), [] ~ trap-seen)`
    - `Fatal error: stack overflow`
  - interpretation:
    - the pure baseline (no manual overrides, no heat/cool) is not yet operational for fib
    - but it now serves as the intended slow/correct-vs-optimized comparison baseline

- Baseline bind-before-use warning groups from `/tmp/bs_rewrite.log`:
  - total rule warnings: 20
  - cast/ref success:
    - `step-read-br-on-cast-succeed`
    - `step-read-br-on-cast-fail-succeed`
    - `step-read-ref-test-true`
    - `step-read-ref-cast-succeed`
  - call-ref:
    - `step-read-call-ref-func`
  - throw:
    - `step-throw`
  - struct:
    - `step-read-struct-new-default`
    - `step-read-struct-get-struct`
    - `step-struct-new`
    - `step-struct-set-struct`
  - array/data:
    - `step-read-array-new-default`
    - `step-read-array-new-data-oob`
    - `step-read-array-new-data-num`
    - `step-read-array-get-array`
    - `step-read-array-copy-le`
    - `step-read-array-copy-gt`
    - `step-read-array-init-data-oob2`
    - `step-read-array-init-data-num`
    - `step-array-new-fixed`
    - `step-array-set-array`

- Baseline context-disable experiments:
  - disabling `step-ctxt-instrs` still leaves
    `step(< fib-state(i32v(1)) | CTORLOCALGETA1(1) >)` with `Fatal error: stack overflow`
  - disabling `step-ctxt-label` still leaves the same overflow
  - disabling all four context rules
    (`ctxt-instrs,ctxt-label,ctxt-handler,ctxt-frame`) still leaves the same overflow
  - conclusion:
    - the overflow is not caused by the baseline direct conditional encoding of the context rules themselves
    - it persists even with all `step-ctxt-*` rules removed

- Baseline stack-overflow root-cause narrowing:
  - `NOP` single-step succeeds in baseline:
    - `step(< fib-state(i32v(1)) | CTORNOPA0 >)` terminates to `eps`
  - singleton `Step-read` examples overflow immediately:
    - `step(< fib-state(i32v(1)) | CTORLOCALGETA1(1) >)`
    - `step(< fib-state(i32v(1)) | CTORGLOBALGETA1(1) >)`
  - helper functions themselves are not the culprit:
    - `$local(fib-state(i32v(1)), 1)` terminates to `i32 0`
    - `$global(fib-state(i32v(1)), 1)` terminates
  - removing `Step/read` and `Step/pure` schema rules did not fix the overflow
  - tightening `step-ctxt-instrs` with a non-empty focus guard (`INSTR =/= eps`) did not fix the overflow
  - a scratch conditional rule with the same `Idx` sort condition and `$local` assignment succeeds immediately
- conclusion:
  - the baseline overflow is not caused by `$local/$global`, `Idx`, or the schema rules alone
  - it is caused by the full direct-conditional `step` rule set itself: Maude's proof search over the overlapping baseline `step` rules explodes even on tiny `Step-read` terms
  - stronger result:
    - the explosion persists even when all baseline `step-ctxt-*` rules are disabled
    - so the direct cause is the full baseline direct-conditional `step` encoding, not heating/cooling replacement via context rules alone

- Focused-step experiment:
  - added a baseline-only intermediate relation:
    - `op focused-step : ExecConf -> ExecConf`
    - `rl [step-enter] : step(EC) => focused-step(EC) .`
  - moved all baseline direct conditional step rules from `step(...)` to `focused-step(...)`
  - result:
    - `step(< fib-state(i32v(1)) | CTORLOCALGETA1(1) >)` still overflows
    - `steps(fib-config(i32v(5)))` still overflows
  - conclusion:
    - the problem is not just “too many rules directly on the symbol `step`”
    - the proof-search explosion persists even with a one-hop indirection

2026-04-24 session summary

Purpose:
- Continue SpecTec-to-Maude baseline work for professor requirement.
- Baseline means direct conditional Maude rules:
  `crl [r] : step(...) => ... if ... /\ step(...) => ... .`
- Baseline should avoid generated heating/cooling and should be close to SpecTec rule shape.

Current baseline implementation:
- Main files:
  - `translator_bs.ml`
  - `main_bs.ml`
  - `output_bs.maude`
- `translator_bs.ml` builds with:
  - `dune build ./main_bs.exe`
- Regeneration command:
  - `dune exec ./main_bs.exe -- wasm-3.0/* > output_bs.maude`
- Load check:
  - `maude -no-banner output_bs.maude`

Important 2026-04-24 changes:
- Removed the `focused-step` experiment from baseline.
  - No `op focused-step`.
  - No `rl [step-enter]`.
- `output_bs.maude` has no baseline heat/cool context rules.
  - No `heat-step-ctxt-*`.
  - No `cool-step-ctxt-*`.
- `Step/ctxt-instrs` no longer uses synthetic `VALS` / `IS` wrappers.
  - Current shape follows SpecTec more directly:
    `step(< Z | VAL INSTR INSTR1 >) => < ZQ | VAL INSTRQ INSTR1 >`
    with condition:
    `all-vals(VAL) = true /\ step(< Z | INSTR >) => < ZQ | INSTRQ > /\ ...`
- Scalar type guards are now preserved for variables that appear in the generated rule text, including RHS and premise-result variables.
  - Example: `Step/ctxt-frame` now generates `SQ : Store` and `FQQ : Frame`.
  - Example: `Step/ctxt-instrs` now generates `ZQ : State`.
- Sequence `hasType(list(instr))` guards are still intentionally omitted in baseline.
  - Reason: they are expensive over associative sequence terms and are not currently required for the scalar-guard correctness issue.
  - `all-vals(VAL) = true` remains because it represents the SpecTec `val*` side condition.

Verification:
- `dune build ./main_bs.exe` succeeds.
- `dune exec ./main_bs.exe -- wasm-3.0/* > output_bs.maude` succeeds.
- `maude -no-banner output_bs.maude` loads.
- Loading still emits existing warnings/advisories for some generated rules; this session did not attempt to eliminate every warning.

Baseline stack-overflow result:
- The direct conditional baseline still stack-overflows for fib model checking.
- Prior narrowing results:
  - `NOP` singleton step succeeds.
  - `LOCAL.GET` and `GLOBAL.GET` singleton `Step-read` examples stack-overflow under the full baseline rule set.
  - `$local(fib-state(i32v(1)), 1)` and `$global(...)` reduce normally by themselves.
  - A scratch rule with similar `Idx` and `$local` conditions succeeds.
  - Disabling `step-ctxt-instrs`, `step-ctxt-label`, or all four `step-ctxt-*` context rules does not remove the overflow.
  - The removed `focused-step` indirection also did not remove the overflow.
- Current conclusion:
  - The direct cause is not `$local`, `$global`, `Idx`, or the context rules alone.
  - The full direct-conditional executable `step` relation causes Maude's conditional rewrite proof search to explode over overlapping rules, even for tiny concrete `Step-read` terms.

Professor-facing wording:
- Short version:
  "교수님 말씀대로 baseline을 heating/cooling 없이 direct conditional `crl`로 만들었고, `-- Step:` context rule 5개는 SpecTec shape에 맞춰 생성됩니다. 그런데 이 baseline은 fib는 물론 `LOCAL.GET` 단일 step에서도 stack overflow가 납니다. `$local/$global` helper나 `Idx` 조건은 단독으로 정상 종료하고, context rule들을 모두 꺼도 overflow가 남기 때문에, 현재 직접 원인은 context rule이 아니라 full direct-conditional `step` rule set에서 Maude의 조건부 rewrite proof search가 폭발하는 것입니다."
- More precise version:
  "`crl`을 쓰면 항상 안 된다는 뜻은 아닙니다. 문제는 SpecTec의 Step relation을 거의 1:1로 하나의 executable `step` relation에 직접 올리는 baseline 방식이 Maude 실행 전략과 맞지 않는다는 점입니다. 따라서 이 baseline은 correctness/reference artifact로는 의미가 있지만, fib model checking 실행용 semantics로는 optimized translator의 heating/cooling 또는 선배 코드처럼 redex를 좁혀 주는 구조가 필요해 보입니다."

Documentation updated:
- `STATUS.md`
  - Added 2026-04-24 baseline update.
  - Updated Step 0 / Step 1 progress.
  - Added professor-facing report wording.
- `paper_plan.md`
  - Marked baseline translator split and context-rule direct `crl` coverage as completed.
  - Recorded direct conditional baseline stack overflow as a risk and planning constraint.
  - Changed the immediate action from old nested-label deadlock to baseline stack-overflow report / optimized path decision.

Next session entry point:
1. If asked to continue baseline debugging, reproduce with the smallest singleton:
   `step(< fib-state(i32v(1)) | CTORLOCALGETA1(1) >)`.
2. Do not reintroduce `focused-step`; it was tested and removed.
3. Do not add hard-coded rule-specific special cases to `translator_bs.ml`.
4. If optimizing, compare against the existing `translator.ml` heat/cool path and the manual senior-code style to identify semantic-preserving redex narrowing.
5. If writing the professor report, use the wording above and emphasize the evidence: helper functions terminate alone, context rules disabled still overflow, singleton `Step-read` still overflows.

2026-04-27 session update

Purpose:
- Continue baseline debugging after professor suggested the stack overflow may be a nil-split problem.
- User asked whether changing the recursive `instr` focus into `W REST` in `translator_bs.ml` would fix it, and requested implementation.

Implementation:
- Modified `translator_bs.ml`.
- For `Step/ctxt-instrs`, the generated recursive focus is now structurally non-empty:
  - old shape:
    `step(< Z | VAL INSTR INSTR1 >) => < ZQ | VAL INSTRQ INSTR1 >`
    with recursive condition `step(< Z | INSTR >) => < ZQ | INSTRQ >`
  - new shape:
    `step(< Z | VAL INSTR-HEAD INSTR-REST INSTR1 >) => < ZQ | VAL INSTRQ INSTR1 >`
    with recursive condition `step(< Z | INSTR-HEAD INSTR-REST >) => < ZQ | INSTRQ >`
  - `INSTR-HEAD : WasmTerminal`
  - `INSTR-REST : WasmTerminals`
- This blocks the empty recursive focus at the left-hand-side pattern level. It directly addresses the professor's `f(nil) f(1 2 3 4)` style nil-split concern.
- Reordered `Step/ctxt-instrs` conditions so cheap guards come before recursive `step`.
- Found a separate issue: generated `Z : State` executable membership guards prevented concrete record-normalized states from matching in baseline `Step*` rules.
  - Current pragmatic fix: `translator_bs.ml` omits executable `: State` guards for `Step*` relations.
  - Interpretation: SpecTec state metavariable sort annotations are treated as generation-time/configuration-shape information, not as a Maude membership proof obligation on every step.
  - Long-term strict fix: generate correct `State` membership for the record representation.
- Added fib harness membership in `wasm-exec-bs.maude`:
  - `mb CTORCONSTA2(CTORI32A0, I:Int) : Val .`

Verification:
- `dune build` succeeds.
- `dune exec ./main_bs.exe -- wasm-3.0/* > output_bs.maude` succeeds.
- Singleton local-get now steps:
  - `rew [1] in WASM-FIB-BS : step(< fib-state(i32v(1)) | CTORLOCALGETA1(1) >) .`
  - result contains `CTORCONSTA2(CTORI32A0, 0)`.
- Baseline fib rewrite now succeeds without stack overflow:
  - `rew [10000] in WASM-FIB-BS : steps(fib-config(i32v(5))) .`
  - result: 24,902 rewrites, final instruction `CTORCONSTA2(CTORI32A0, 5)`.
- Baseline modelCheck is still not usable:
  - `modelCheck(fib-config(i32v(2)), <> result-is(1))` timed out after 60 seconds.
  - So the current remaining baseline problem is model-checking proof-search/state-space cost, not stack overflow during rewrite.

Current professor-facing summary:
- "교수님 말씀대로 nil-split 문제가 맞았습니다. `Step/ctxt-instrs`의 recursive focus가 sequence 변수 하나라 `eps`로도 match될 수 있었고, 이것이 empty-focus proof search를 만들었습니다. `INSTR-HEAD : WasmTerminal` + `INSTR-REST : WasmTerminals`로 바꿔 focus가 항상 non-empty가 되도록 수정했습니다."
- "수정 후 `LOCAL.GET` 단일 step과 `fib(5)` rewrite는 stack overflow 없이 종료합니다."
- "다만 direct `crl` baseline modelCheck는 `fib(2)`에서도 60초 안에 끝나지 않았습니다. 그래서 지금 남은 문제는 stack overflow가 아니라 model-checking 단계의 상태공간/proof-search 비용입니다."

Next session entry point:
1. Do not revert the nil-split fix in `translator_bs.ml`.
2. If strictness is requested, work on proper `State` membership generation instead of re-adding raw `Z : State` guards to `Step*` rules.
3. For modelCheck performance, compare the direct-crl baseline with `translator.ml` heat/cool and senior-code redex-localized structure.
4. If reporting, say: nil-split fixed rewrite; modelCheck still expensive.

2026-04-28 session update

Purpose:
- Continue C1 baseline work after the professor required relation-preserving
  `Step`, `Step_pure`, `Step_read`, and `Steps`.
- User asked specifically to:
  1. preserve `val^n` binders generally in `translator_bs.ml`,
  2. make `Step_pure/br-label-zero` rewrite inside wrappers,
  3. re-run `fib(0)`, `fib(1)`, `fib(5)` rewriting/model checking,
  4. update documents.

Implementation:
- `lib/frontend/elab.ml`
  - Definite iterator binders are now preserved instead of being lowered to
    ordinary `List`.
  - This lets the backend see `ListN(count, ...)` for binders such as `val^n`.
- `translator_bs.ml`
  - Records general `(count, sequence)` pairs from `ListN` binders.
  - Generates length constraints such as `len(VAL) == N`.
  - `Step_pure/br-label-zero` now has the generated `len(VAL) == N` condition.
  - Removed generic `Step/pure` and `Step/read` bridge rules because they could
    satisfy conditions with unreduced relation calls and accumulate wrappers.
  - Generates per-case bridges for `Step_pure` and `Step_read`.
  - Generates prefix/suffix context bridges mechanically for `Step_pure`,
    `Step_read`, direct `Step` rules, and label/handler/frame contexts.
  - The executable generic `Step/ctxt-instrs` split rule is currently omitted
    because it causes Maude list-split explosion.
- `wasm-exec-bs.maude`
  - `exec-step` uses config projection helpers, but it still relies on the
    conditional form `step(C) => C'`.

Verification:
- `dune build` succeeds.
- `dune exec ./main_bs.exe -- wasm-3.0/* > output_bs.maude` succeeds.
- Rewriting now succeeds:
  - `fib(0)` -> `CTORCONSTA2(CTORI32A0, 0)`, 2,163 rewrites, about 0.29s real.
  - `fib(1)` -> `CTORCONSTA2(CTORI32A0, 1)`, 7,213 rewrites, about 0.29s real.
  - `fib(5)` -> `CTORCONSTA2(CTORI32A0, 5)`, 27,593 rewrites, about 0.31s real.
- Model checking:
  - `modelCheck(fib-config(i32v(0)), <> result-is(0))` returns `true`.
    It used 2,567,549 rewrites and about 1.39s real.
  - `fib(1)` reachability timed out at 60s.
  - `fib(5)` reachability timed out at 60s.
- Diagnostic search:
  - `search [20] in WASM-FIB-BS : fib-config(i32v(1)) =>1 C:Config .`
    produced at least 20 one-step solutions in about 3.28s.
  - Current diagnosis: model checking still explodes because the harness rule
    uses `step(C) => C'` in a Maude rewrite condition, which can expose many
    reachable `Config` results instead of only the intended immediate step.

Documents updated:
- `STATUS.md`
- `paper_plan.md`
- `.codex.md`
- `meeting/session_summary.txt`

Current professor-facing summary:
- "`val^n` 보존과 `Step_pure/br-label-zero` wrapper 내부 rewrite 문제는
  해결했습니다. baseline rewrite는 `fib(0)`, `fib(1)`, `fib(5)` 모두 정상
  종료합니다."
- "다만 model checking은 `fib(0)`만 true로 끝나고 `fib(1)`부터 60초
  timeout입니다. 현재 원인은 계산 규칙이 stuck되는 것이 아니라 `exec-step`의
  `step(C) => C'` 조건이 immediate one-step이 아니라 여러 reachable config를
  transition으로 노출하는 데 있습니다."

Next session entry point:
1. Do not revert the `val^n` preservation in `lib/frontend/elab.ml` and
   `translator_bs.ml`.
2. Do not reintroduce the generic `Step/pure` or `Step/read` bridge.
3. Do not reintroduce executable generic `Step/ctxt-instrs` without measuring
   state explosion.
4. Next technical task is to make `exec-step` a true one-step transition for
   model checking.

2026-04-28 strict C1 update after professor direction

Purpose:
- Follow the professor's requested C1 baseline shape strictly:
  `Step`, `Step_pure`, `Step_read`, and `Steps` should remain separate
  relations; `steps(C) => C'` should be used; configs should print as `_ ; _`.
- Re-test whether the faithful baseline can rewrite and model-check Fibonacci.

Implementation:
- `translator_bs.ml`
  - Keeps `Step`, `Step_pure`, `Step_read`, and `Steps` as separate generated
    Maude operators.
  - Generates `Step/pure` through `step-pure(...)` and `Step/read` through
    `step-read(...)`.
  - Generates `Steps/trans` as `steps(C) => C'' if step(C) => C' /\ steps(C') => C''`.
  - Adds `ValTerminals` and declares SpecTec `val*` binder variables with that
    sort instead of arbitrary `WasmTerminals`.
  - Preserves `val^n` as length constraints.
  - Keeps `Step/ctxt-instrs` as a generic context rule but rewrites the
    recursive focus to a non-empty sequence `INSTR-HEAD INSTR-REST`; this blocks
    the nil-split case where the focus is `eps`.
  - Splits empty/non-empty result cases where Maude needs separate rules for
    sequence result `eps`.
- `wasm-exec-bs.maude`
  - Adds `MCConfig` and `mc(Config)` for model checking.
  - `exec-step` rewrites `mc(C)` to `mc(C')`, not raw `Config` to `Config`.
  - This prevents the harness transition from being reused recursively while
    Maude solves `step(C) => C'`.

Verification:
- `dune build` succeeds.
- `dune exec ./main_bs.exe -- wasm-3.0/* > output_bs.maude` succeeds.
- Direct previously stuck label+suffix case now rewrites:
  - 156 rewrites, about 1ms Maude real time.
- C1 rewriting:
  - `fib(0)` -> `i32v(0)`, 945 rewrites, about 2ms Maude real.
  - `fib(1)` -> `i32v(1)`, 7,549 rewrites, about 10ms Maude real.
  - `fib(5)` -> `i32v(5)`, 33,965 rewrites, about 44ms Maude real.
  - `steps(fib(5))` -> `i32v(5)`, 33,966 rewrites, about 45ms Maude real.
- Terminal-state model-check sanity:
  - `mc(fib-state(i32v(0)) ; CTORCONSTA2(CTORI32A0, 0)) |= result-is(0)` is `true`.
  - `modelCheck(mc(fib-state(i32v(0)) ; CTORCONSTA2(CTORI32A0, 0)), <> result-is(0))`
    is `true`.
- Initial-state model checking:
  - `modelCheck(mc(fib-config(i32v(0))), <> result-is(0))` timed out after
    120 seconds.
  - Search from `mc(fib-config(i32v(0)))` timed out after 20 seconds after only
    a few states and showed nested label/control contexts.

Current diagnosis:
- The previous stack-overflow / nil-split / rewrite stuckness issue is fixed.
- The previous raw-`Config` model-check harness over-approximation is also
  fixed by `mc(Config)`.
- The remaining failure is C1 state-space/proof-search explosion during model
  checking. This is evidence that C1 should be the faithful reference semantics
  and C2 should be the practical model-checking semantics.

Next session entry point:
1. Report to the professor that strict C1 rewrite/`steps` works but direct C1
   model checking still times out.
2. Ask whether C1 must model-check from initial states, or whether C1 as a
   faithful executable reference plus C2 as optimized model-check target is the
   intended research split.
3. Start C2 from the current C1 and measure model-checking improvement.
