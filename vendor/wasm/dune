(include_subdirs unqualified)

(library
  (name wasm)
  (modules :standard \ main wast smallint)
)

(env (_ (flags (-w +a-4-27-42-44-45-70 -warn-error +a-3))))

(subdir text
  (ocamllex (modules lexer))
  (menhir (modules parser))
)
